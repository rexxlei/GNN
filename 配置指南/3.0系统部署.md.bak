# 3.0系统部署
## 文件位置：
* `config`放在`/home/cotek`下
* `3d_slam`的驱动放置见readme文件
## 启动前工作
#### 1. 获取tracer_ros源码
```bash
git clone https://github.com/agilexrobotics/ugv_sdk.git
git clone https://github.com/agilexrobotics/tracer_ros.git
```
* 修复工作空间编译报错：
缺少asio.hpp:安装独立版Asio

```bash
sudo apt-get update
sudo apt-get install -y libasio-dev
# 验证头文件确实在系统里
dpkg -L libasio-dev | grep '/asio.hpp$'
```

接着`ugv_sdk/CMakeLists.txt`里，添加在 `add_library{}(ugv_sdk ...) `之后即可：

```cmake
# 告诉编译器：用“独立版 Asio”，而非 Boost.Asio
target_compile_definitions(ugv_sdk PUBLIC ASIO_STANDALONE)

# 显式加入 Asio 头文件路径（通常 /usr/include，稳一点就查找）
find_path(ASIO_INCLUDE_DIR asio.hpp PATHS /usr/include /usr/local/include)
if(NOT ASIO_INCLUDE_DIR)
  message(FATAL_ERROR "asio.hpp not found; please apt-get install libasio-dev")
endif()
target_include_directories(ugv_sdk PUBLIC ${ASIO_INCLUDE_DIR})

# Asio 用到线程，链接 pthread（很多工程已经链了；没链就补上）
target_link_libraries(ugv_sdk PUBLIC pthread)
```
* 安装依赖`genmsg`
```bash
sudo apt-get install ros-noetic-genmsg ros-noetic-gencpp ros-noetic-genpy
```
#### 2. 修改`tracer_ros`源代码以适配can  
* 拉取`tracer_base_node.cpp`  
通过gitclone拉取代码  
* 修改`tracer_base/cmakelist`
确保包含以下内容：
```cmake
include_directories(
  include
  ${catkin_INCLUDE_DIRS}
  /usr/local/include  # .h文件的系统路径，可拷贝到该路径下
  ~/linux-demo-ver\ 2.0.8-1.0.23/include  # ICANCmd.h实际所在的路径
)

link_directories(  # libCanCmd.so所在的系统路径和实际路径
  /usr/local/lib
  ~/linux-demo-ver\ 2.0.8-1.0.23/lib
)

add_executable(tracer_base_node src/tracer_base_node.cpp)
target_link_libraries(tracer_base_node
  ${catkin_LIBRARIES}
  CanCmd  # libCanCmd.so
)
```
* 将ICANCmd.h复制到`tracer_base/include`下

#### 3. 创建虚拟vcan来实现can数据传输
* 在终端创建并激活vcan0  
编写打开vcan0的service  
```bash
sudo nano /etc/systemd/system/vcan0.service
```
粘贴以下内容
```ini
[Unit]
Description=Virtual CAN interface vcan0
After=network.target
Wants=network.target

[Service]
Type=oneshot
ExecStart=/sbin/ip link add dev vcan0 type vcan
ExecStartPost=/sbin/ip link set vcan0 up
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```
重载配置并打开服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable vcan0.service
sudo systemctl start vcan0.service
```
* 创建`tracer_bridge.cpp`实现vcan与SDK的数据互传
* 创建一个`tracer_bridge.launch`来同时启动`tracer_bridge`,`tracer_base`和`tracer_keyboard`

#### 4. 设置开机自运行脚本
* 启用 rc.local 服务
```bash
sudo systemctl enable rc-local.service
```
* 创建 /etc/rc.local 文件
```bash
sudo nano /etc/rc.local
```
* 文件内容示例
```bash
#修改网口ip地址
sudo ip addr flush dev eth2
sudo ip addr add 192.168.1.50/24 dev eth2
sudo ip link set eth2 up

#打开vcan0
sudo systemctl start vcan0.service

#设置wifi网口ip地址
sudo dhclient eth3
```


#### 5. 将编译结果安装到系统路径
* 先赋予rosout权限：
```bash
sudo chmod +x /opt/ros/noetic/lib/rosout/rosout
```
* 添加环境变量  
在`.bashrc`末尾添加：
```
source ~/cotek/cotek_ws/devel/setup.bash
```
* 将编译结果安装到系统
```bash
cd ~/cotek/cotek_ws/build
sudo make
sudo make install
```

#### 6. 换系统源
* 查看当前系统源：
```bash
sudo cat /etc/apt/sources.list
```
* 换系统源：
```bash
sudo sh -c 'cat > /etc/apt/sources.list << EOF
deb https://mirrors.ustc.edu.cn/ubuntu/ focal main restricted universe multiverse
deb https://mirrors.ustc.edu.cn/ubuntu/ focal-updates main restricted universe multiverse
deb https://mirrors.ustc.edu.cn/ubuntu/ focal-backports main restricted universe multiverse
deb https://mirrors.ustc.edu.cn/ubuntu/ focal-security main restricted universe multiverse
EOF'
```


## tplink网桥部署：

将连接的网口设置为自动获取ip地址（dhc）
命令行:（以eth0为例）
```bash
sudo dhclient -r eth0
sudo dhclient eth0
```

## can口驱动

找到80-laike-acusb.rules所在文件夹
```bash
sudo cp 80-laike-acusb.rules /etc/udev/rules.d/
```
测试：  
进入64bit文件夹：
```bash 
sudo make install 
sudo make
sudo ./testLikeCan
```

测试can口是否连接：
```bash
candump can0
```

## 配置vpn
解压u盘中clash压缩包，在u盘中打开终端：
```bash
sudo mv Clash\ for\ Windows-0.20.39-x64-linux /opt/clash
```
打开clash：
```cd /opt/clash
./cfw
```
配置桌面图标：
```bash
cd ~/.local/share/applications
nano clash_gui.desktop
```
添加如下内容：  
```
[Desktop Entry]
Name=clash for linux
Icon=/opt/clash/clash.png
Exec=/opt/clash/cfw
Type=Application
```
最后增加权限：
```bash
sudo chmod a+x clash_gui.desktop
```

在网络--网络代理中切换为手动
```
HTTP代理：127.0.0.1  7890  
HTTPS代理：127.0.0.1  7890  
Socks主机：127.0.0.1  7891
```
若不使用网络代理，将‘手动’改为‘关闭’即可